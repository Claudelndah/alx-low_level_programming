#include "main.h"

/**
 * print_last_digit - print the las digit of a number
 * @i: the number that print the last digit
 * Return: the last digit (Success)
 */

int print_last_digit(int i)
{
	return (i % 10);
}
